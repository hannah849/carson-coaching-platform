export default function AdminPage() {
  return (
    <div>
      <h1 className="font-serif text-4xl text-[var(--foreground)] mb-6">Admin</h1>
      <p className="text-[var(--muted)]">Placeholder — Phase 8 will build this out.</p>
    </div>
  );
}
